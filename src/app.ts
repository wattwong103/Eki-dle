/**
 * Placeholder until `node scripts/assemble-app.mjs` runs (npm predev/pretest/prebuild).
 * Source: scripts/app-src/app.ts.gz.b64.p0 + .p1 (gzip+base64 of full app.ts).
 */
export async function boot(): Promise<void> {
  throw new Error("src/app.ts not assembled — run: node scripts/assemble-app.mjs");
}
